import { MapSelectItemPipe } from './map-select-item.pipe';

describe('MapSelectItemPipe', () => {
  it('create an instance', () => {
    const pipe = new MapSelectItemPipe();
    expect(pipe).toBeTruthy();
  });
});
